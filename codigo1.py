#Questao 1 - Processo seletivo Target.
#Optei pela linguagem Python.

indice, soma, k = 13, 0, 0  #declaração das variáveis.
while (k < indice):         #estrutura de repetição.
    k +=1
    soma = soma + k
print (soma)                #saída do resultado.